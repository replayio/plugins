node_modules/
# NOTE: `dist/` is intentionally checked in. CI verifies it is up-to-date.
#
# If you regenerate and `dist/` changes, update via `npm run build` and commit.
#
# (We still keep `release-assets/` ignored since it is CI output.)
release-assets/
.npm/

.DS_Store
*.log
